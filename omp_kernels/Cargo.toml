[package]
name = "neuralops_omp_kernels"
version = "0.0.1"
authors = ["Peter Jin <peterhaijin@gmail.com>"]
build = "build.rs"

[features]
iomp = []

[lib]
name = "neuralops_omp_kernels"
path = "lib.rs"

[build-dependencies]
gcc = "*"

[dependencies]
libc = "*"
